<p id="notice"><%= notice %></p>

<h1>Provinces</h1>

<br>

<%= button_to 'New Province/Division', new_province_path, method: :get %>

<p/>

<table>
  <thead>
    <tr>
      <th>Index</th>
      <th>Name</th>
      <th>Admin Division</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @provinces.each_with_index do |province,i| %>
      <tr>
        <td><%= i+1 %></td>
        <td><%= province.name %></td>
        <td><%= province.admlevel %></td>
        <td><%= link_to 'Show', province %></td>
        <td><%= link_to 'Edit', edit_province_path(province) %></td>
        <td><%= link_to 'Remove', province, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<p/>

<%= button_to 'New Province/Division', new_province_path, method: :get %>
